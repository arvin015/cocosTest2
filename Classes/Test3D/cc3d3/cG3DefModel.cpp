#include "cG3DefModel.h"


void cG3DefModel::draw(){
	CCASSERT(false, "should Not this draw function in standard CC2DX");
	/*
    static const size_t v3ssize = sizeof(vec3short_t);
    static const size_t vmvsize = sizeof(Vec2);
    static const size_t v3size = sizeof(Vec3);
    //glNormalPointer(GL_SHORT, v3ssize, normalArray[i][0]);
    glNormalPointer(GL_FLOAT, v3size, nors);
    glTexCoordPointer (2, GL_SHORT, vmvsize, uvs);
    glVertexPointer (3, GL_FLOAT, v3size, vers);	
    glDrawElements (GL_TRIANGLES, num_idx, GL_UNSIGNED_SHORT, idxs);
	*/
}
